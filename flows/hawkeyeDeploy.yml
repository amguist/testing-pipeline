Deploy:
  - checkpoint: "Start"
  - form: myForm
    values:
      processId: ${txId}
      repo: ${repo}
      branch: ${branch}
      kittFiles: ${kittFiles}
  - log: "Hawkeye Concord | Kitt Files: ${kittFiles}"
  - log: "Hawkeye Concord | Selected Kitt Files: ${myForm.onlySelectedKittFiles}"


