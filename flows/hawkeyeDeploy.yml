Deploy:
  - form: myForm
    values:
      processId: ${txId}
      repo: ${repo}
      branch: ${branch}
      kittFiles: ${kittFiles}
  - log: "Organization: ${myForm.onlyDeployOrg}"
  - log: "Kitt Files Selected: ${myForm.onlySelectedKittFiles}"