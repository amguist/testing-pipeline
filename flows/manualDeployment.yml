Manuel_Deployment:
  - form: manualDeploymentForm
    fields:
      - repo: { label: "Repository", type: "string", readonly: true, allow: "main"  }
      - branch: { label: "Branch:", type: "string", readonly: true }
      - pipelineFlow: { label: "Flow", type: "string" }

  - log: "Concord | Invoking Flow: (manualDeploymentForm.pipelineFlow)"